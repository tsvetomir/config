#!/bin/bash
for param in "$@"
do
    ffmpeg -i "$param" \
           -c:v libx264 -b:v 10M -profile:v main -level 4.0 \
           -strict -2 \
           -c:a aac -b:a 128k \
           -movflags +faststart \
           -map_metadata 0 \
           "${param%.*}.mp4"
done
